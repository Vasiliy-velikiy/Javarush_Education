package JavaRushLevel18;

import org.w3c.dom.ls.LSOutput;

import java.io.BufferedReader;
import java.io.InputStreamReader;

public class Test {
    public static void main(String[] args) {
        /*
        String se="Куртка для сн";
        String se2="Куртка для сноубордистов, размерище";
        String se3="151233329.00";


        if(args[0].equals("-c")) {
            String productname = "";
            String spase = "                                             ";
            if (se.length() < 30) {
                productname = se + spase.substring(se.length(), 29) + "rer";
            }

            System.out.println(productname);


            if (se2.length() >= 30) {
                productname = se2.substring(0, 30);
            }
            System.out.println(productname);


            String price = "";
            if (se3.length() < 8) {
                price = se3 + spase.substring(se3.length(), 8) + "keke";
            }

            if (se3.length() > 8) {
                price = se3.substring(0, 8);
            }
            System.out.println(price);
        }


         */


        BufferedReader bf=new BufferedReader(new InputStreamReader(System.in));
    }
}
